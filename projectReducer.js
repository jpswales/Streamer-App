const iniState = {
    films: [
        {id:'1', title: 'testing one two three', synopsis: 'ddd', genre: 'hhh'}
    ]
}

const projectReducer = (state = initState, action) => {


    console.log("Reduced");
    return state;
}